import Image from "next/image"; import Link from "next/link"; import type {Product} from "@/data/products"; import {categories} from "@/data/categories"; import styles from "./ProductCard.module.css";
export function ProductCard({product,large=false}:{product:Product;large?:boolean}){const category=categories.find(c=>c.slug===product.category)?.name;const sizes=large?"(max-width: 800px) 100vw, 44vw":"(max-width: 800px) 46vw, (max-width: 1280px) 29vw, 22vw";return <article className={`product ${large?'product-large':''}`}><Link href={`/san-pham/${product.slug}`} className={`product-image ${styles.image}`}><Image src={product.cover} alt={product.name} fill sizes={sizes}/></Link><div className={`product-copy ${styles.copy}`}><p>{category}</p><h3><Link href={`/san-pham/${product.slug}`}>{product.name}</Link></h3><span>{product.priceLabel}</span></div></article>}
