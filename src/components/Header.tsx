"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {Menu} from "./Icons"; import {business,zaloHref} from "@/data/business";
export function Header(){const [open,setOpen]=useState(false); const links=[['Trang chủ','/'],['Hoa','/san-pham'],['Về Tamas','/ve-tamas'],['Liên hệ','/lien-he']];useEffect(()=>{const close=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false);window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[]);return <header className="site-header"><Link href="/" className="wordmark" aria-label="Tamas Flower Store trang chủ">Tamas<span>Flower Store</span></Link><nav aria-label="Điều hướng chính">{links.map(([n,h])=><Link key={n} href={h}>{n}</Link>)}<a href={business.instagram} target="_blank" rel="noopener noreferrer">Instagram</a></nav><a className="header-cta" href={business.facebook} target="_blank" rel="noopener noreferrer">Đặt qua fanpage</a><button className="menu-button" aria-label={open?"Đóng menu":"Mở menu"} aria-controls="mobile-menu" aria-expanded={open} onClick={()=>setOpen(!open)}><Menu/></button>{open&&<div id="mobile-menu" className="mobile-menu">{links.map(([n,h])=><Link key={n} href={h} onClick={()=>setOpen(false)}>{n}</Link>)}<a href={business.instagram} target="_blank" rel="noopener noreferrer">Instagram</a><a className="button button-rose" href={business.facebook} target="_blank" rel="noopener noreferrer">Đặt qua fanpage</a><a className="text-link" href={zaloHref()} target="_blank" rel="noopener noreferrer">Nhắn Zalo</a></div>}</header>}
